import React from 'react';

const LoginReminder = ({ message }) => {
    return (
        <div className="login-reminder">
            <p>{message}</p>
        </div>
    );
};

export default LoginReminder;
